{% set tpl = ix_lib.base.render.Render(values) %}

{% set c1 = tpl.add_container(values.consts.parson_container_name, "image") %}
{% set perm_container = tpl.deps.perms(values.consts.perms_container_name) %}
{% set perms_config = {"uid": 10001, "gid": 10001, "mode": "check"} %}

{% do c1.set_user(10001, 10001) %}
{% do c1.healthcheck.set_test("curl", {"port": values.network.web_port.port_number, "path": "/health"}) %}

{% do c1.environment.add_env("RUNNING_IN_DOCKER", "true") %}
{% do c1.environment.add_env("PARSON_BIND_ADDRESS", "0.0.0.0") %}
{% do c1.environment.add_env("PARSON_PORT", values.network.web_port.port_number) %}
{% do c1.environment.add_user_envs(values.parson.additional_envs) %}

{% do c1.add_port(values.network.web_port) %}
{% do c1.add_storage(values.consts.data_path, values.storage.data) %}
{% do c1.add_storage(values.consts.music_path, values.storage.music) %}
{% do perm_container.add_or_skip_action("data", values.storage.data, perms_config) %}

{% if perm_container.has_actions() %}
  {% do perm_container.activate() %}
  {% do c1.depends.add_dependency(values.consts.perms_container_name, "service_completed_successfully") %}
{% endif %}

{% do tpl.portals.add(values.network.web_port) %}

{{ tpl.render() | tojson }}
